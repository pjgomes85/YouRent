


Installation:

-Git Clone repo

-In command line, run :

Bundle Install
npm install
yarn add @rails/actioncable
yarn add js-autocomplete
yarn add @mapbox/mapbox-gl-geocoder

Rails DB Create
Rails DB Migrate
